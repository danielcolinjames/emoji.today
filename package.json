{
  "name": "emoji-today-monorepo",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*",
      "lib/*"
    ]
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "lfg": "turbo run dev --filter=emoji.today",
    "lfg:build": "turbo run build --filter=emoji.today",
    "lfg:cleanup": "turbo run cleanup --filter=emoji.today",
    "staging": "turbo run dev --filter=staging.emoji.today",
    "staging:build": "turbo run build --filter=staging.emoji.today",
    "admin": "turbo run dev --filter=admin.emoji.today",
    "admin:build": "turbo run build --filter=admin.emoji.today"
  },
  "devDependencies": {
    "@types/range-parser": "^1.2.7",
    "turbo": "latest"
  },
  "packageManager": "yarn@1.22.22",
  "dependencies": {
    "node-vibrant": "4.0.3"
  },
  "resolutions": {
    "next": "15.0.3",
    "@neynar/react/next": "15.0.3"
  }
}
